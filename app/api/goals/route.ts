import { NextResponse } from "next/server"; import { currentSession } from "@/lib/auth/session"; import { hasSafeOrigin, jsonError, readJson } from "@/lib/auth/http"; import { firstValidationMessage } from "@/lib/auth/validation"; import { prisma } from "@/lib/db/prisma"; import { goalInputSchema } from "@/lib/planner/validation";
export async function GET() { const session = await currentSession(); if (!session) return jsonError("Silakan masuk terlebih dahulu.", 401); const goals = await prisma.goal.findMany({ where: { userId: session.userId }, include: { tasks: { where: { deletedAt: null }, select: { id: true, title: true, status: true } } }, orderBy: [{ monthStart: "desc" }, { createdAt: "desc" }] }); return NextResponse.json({ ok: true, goals }); }
export async function POST(request: Request) { if (!hasSafeOrigin(request)) return jsonError("Origin permintaan tidak valid.", 403); const session = await currentSession(); if (!session) return jsonError("Silakan masuk terlebih dahulu.", 401); const parsed = goalInputSchema.safeParse(await readJson(request)); if (!parsed.success) return jsonError(firstValidationMessage(parsed.error)); const i = parsed.data; const goal = await prisma.goal.create({ data: { userId: session.userId, title: i.title, description: i.description || null, monthStart: new Date(`${i.monthStart}T00:00:00.000Z`), targetAt: i.targetAt, status: i.status, progress: i.progress } }); return NextResponse.json({ ok: true, goal }, { status: 201 }); }
